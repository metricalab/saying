server:
  port:
    8090
  servlet:
    context-path: 
              /metrica
logging:
  level:
    org:
      hibernate:
        SQL:
          debug
    root:
      INFO
  file:
    saying-local.log
        
spring:
  datasource:
    url: 
      jdbc:mysql://host.docker.internal/db_sayings?useSSL=false
    username:
      root
    password:
      metrica123
    driver-class-name:
      org.mariadb.jdbc.Driver
  jpa:
    database-platform: 
      org.hibernate.dialect.MariaDB103Dialect
    hibernate.ddl-auto:
      create-drop      

metrica:
  data:
    # Possible Values: dataBase | inMemory
    service:
        dataBase
       

      
      
   
  
    
